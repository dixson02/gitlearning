command for chking git veriosn

>git --version

command for listing configuration

>git config --list

command for adding user name

>git config --global user.name "username"

command for adding user email

>git config --global user.email "useremail"

command for initializing git in pwd

>git init

command for listing all untracked files

>git status

command for adding untracked files to staging area

>git add -A

command for adding staged files to local repository

>git commit -m "commit message"

command for adding local repository content to remote

>git push 

exact copy from remote repository
>git clone

remote repository chnges
>git pull

command for add remote
>git remote add remoteName path

listing remote

>git remote -v

command for listing all branches
>git branch

command for checkout to a branch

>git checkout branchname


